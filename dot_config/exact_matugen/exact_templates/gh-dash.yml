# Generated by matugen. Do not edit by hand.
# gh-dash supports an `include` directive. In your ~/.config/gh-dash/config.yml add:
#   include:
#     - ~/.config/gh-dash/matugen.yml
# and REMOVE the inline `theme:` block from config.yml (the including file wins on
# conflicts, so an inline theme would override this one).
theme:
  ui:
    sectionsShowCount: true
    table:
      showSeparator: false
      compact: false
  colors:
    text:
      primary: "{{ base16.base05.default.hex }}"
      secondary: "{{ base16.base0b.default.hex }}"
      inverted: "{{ colors.surface.default.hex }}"
      faint: "{{ colors.secondary.default.hex }}"
      warning: "{{ base16.base0a.default.hex }}"
      success: "{{ base16.base0b.default.hex }}"
      error: "{{ base16.base08.default.hex }}"
      actor: "{{ base16.base0d.default.hex }}"
    background:
      selected: "{{ base16.base01.default.hex }}"
    border:
      primary: "{{ base16.base01.default.hex }}"
      secondary: "{{ colors.outline.default.hex }}"
      faint: "{{ base16.base01.default.hex }}"
