# Generated by matugen. Do not edit by hand.
# gh-dash supports an `include` directive. In your ~/.config/gh-dash/config.yml add:
#   include:
#     - ~/.config/gh-dash/matugen.yml
# and REMOVE the inline `theme:` block from config.yml (the including file wins on
# conflicts, so an inline theme would override this one).
theme:
  ui:
    sectionsShowCount: true
    table:
      showSeparator: false
      compact: false
  colors:
    text:
      primary: "#d3c6aa"
      secondary: "#83c092"
      inverted: "#2b3339"
      faint: "#7393b3"
      warning: "#dbbc7f"
      success: "#83c092"
      error: "#e67e80"
      actor: "#7393b3"
    background:
      selected: "#323c41"
    border:
      primary: "#323c41"
      secondary: "#4e565c"
      faint: "#323c41"
